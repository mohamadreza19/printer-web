import styled from "styled-components";

export default styled.div`
  //   transform: scale(${(prop) => prop.scale});
  zoom: ${(prop) => prop.scale};
  width: fit-content;
  padding: 135px 100px 0 100px;
`;
